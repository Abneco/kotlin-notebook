#%% some
<info descr="Inject code for supported libraries: artifact resolution, default imports, initialization code, type renderers">%use xyz</info>
<info descr="Log classpath changes. Useful for debugging artifact resolution failures">%trackClasspath</info>
