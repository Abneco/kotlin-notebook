// "Create extension function" "true"
class Hohoh<T>(elems: Collection<T>) { private val data = mutableListOf<T>()
 init { data.addAll(elems) } }
val h = Hohoh(listOf(1, 2, 42))

fun <E> Hohoh<E>.foo() {
TODO("Not yet implemented")
}
